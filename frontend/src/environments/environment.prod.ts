export const environment = {
  // TODO : remplacer par l'URL réelle du backend de production avant le déploiement.
  // Tant que "REPLACE-ME" reste présent, main.ts refuse de démarrer l'app
  // (voir la vérification en tête de src/main.ts) au lieu d'échouer
  // silencieusement sur chaque appel API.
  apiUrl: 'https://api.REPLACE-ME.example.com',
};
